* 全选文件，将文件统一命名按序号排序（例如，输入 ss ，则呈现 ss(01) ss(02) ...)

* 打开Excel，把修改后的文件名输入A列并加上引号（输入一个后，下拉右下角的格子，会自动递增），把需要最终修改的文件名输入B列，
  把修改命令 "ren "&A1&" "&B1 输入C列上方 fx 旁边的输入框，选中单元格回车，然后下拉格子
  
* 复制 C 列数据到 ren.txt 文档，将后缀修改为 bat ，在当前文件目录下运行即可
